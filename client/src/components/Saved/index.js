
import React from "react";

function Saved() {
  return (
    <div>
      <h1>Saved Books</h1>
      <p>
            Saved Books Go Here
            {/* If user clicks the SAVED button, then push that book into the SAVED array. Display that saved array here */}
      </p>
    </div>
  );
}

export default Saved;